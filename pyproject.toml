[project]
name = "torch-env"
version = "0.1.0"
description = "pytorch training environment"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "torch",
    "matplotlib",
    "torchvision",
    "timm",
    "pandas",
    "tqdm",
    "ipykernel",
    "jupyter"
]
